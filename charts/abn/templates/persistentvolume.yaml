apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Name }}
  {{ template "iter8-abn.labels" . }}
spec:
  storageClassName: hostpath
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/config"