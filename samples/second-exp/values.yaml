baseline:
  dynamic:
    tag: "1.0"
    # see explanation for id below
    id: "8nlos"
candidate:
  # when promoting a candidate version, copy candidate.dynamic into baseline.dynamic
  # and make the candidate field null
  dynamic:
    tag: "2.0"
    # id is a random alpha-numeric value that is uniquely associated with a given version
    # change candidate.dynamic.id every time anything else changes 
    # in the candidate section of Helm chart values
    id: "kj8a8"
