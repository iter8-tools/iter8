baseline:
  dynamic:
    tag: "1.0"
candidate:
  dynamic:
    tag: "2.0"
    # random alpha-numeric value which should be changed
    # every time anything changes in the candidate section of Helm chart values
    hash: "kj8a8"
