apiVersion: v2
name: deploy
description: A Helm chart for Kubernetes deployment with SLO validation
type: application
version: 0.1.0
appVersion: 0.1.0
dependencies:
  - name: experiment
    repository:
    version: 0.1.0
    # create an experiment only if both candidate and experiment sections
    # are present in values.yaml
    condition: candidate,experiment