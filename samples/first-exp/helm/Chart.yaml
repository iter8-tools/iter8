apiVersion: v2
name: deploy
description: A Helm chart for Kubernetes deployment with SLO validation
type: application
version: 0.1.0
appVersion: 0.1.0
dependencies:
- name: deploy # Helmex chart
  repository: https://iter8-tools.github.io/iter8/
  version: 0.1.0
  # release deploy helmex chart only if both candidate and experiment sections
  # are present in values.yaml
  condition: candidate,experiment
