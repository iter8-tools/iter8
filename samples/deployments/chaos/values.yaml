# Type of kubernetes workload
appkind: deployment

# Mean latency SLO upper limit
limitMeanLatency: 50.0 # msec

# Error rate SLO upper limit
limitErrorRate: 0.01 # 1% error is ok

# 95th percentile tail latency SLO upper limit
limit95thPercentileLatency: 100.0 # msec
