apiVersion: v1
kind: Service
metadata:
  name: {{ include "firsttest.fullname" . }}
  labels:
    {{- include "firsttest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firsttest.selectorLabels" . | nindent 4 }}
