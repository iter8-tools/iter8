[build]
base = "mkdocs/"
publish = "site/"
command = """
npm install && 
npm run build &&
python3 -m venv .venv &&
source .venv/bin/activate &&
pip3 install -r requirements.txt && 
mkdocs build
"""

[build.environment]
PYTHON_VERSION = "3.7"
