apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Chart.Name }}-{{ .Release.Revision }}-configmap-updater
subjects:
- kind: ServiceAccount
  name: default
roleRef:
  kind: Role
  name: {{ .Chart.Name }}-{{ .Release.Revision }}-configmap-updater
  apiGroup: rbac.authorization.k8s.io
