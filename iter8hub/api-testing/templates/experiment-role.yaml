apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Chart.Name }}-{{ .Release.Revision }}-configmap-updater
rules:
- apiGroups: [""]
  resources: ["configmaps"]
  resourceNames: ["{{ .Chart.Name }}-{{ .Release.Revision }}-results"]
  verbs:
  - get
  - create
  - patch